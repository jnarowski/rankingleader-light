if !@project_rank.errors.empty?
  page.replace_html 'ranking-metrics-box', :partial => 'forms/project_rank'
else
  page.replace_html 'ranking-metrics-box', :partial => 'projects/show_ranking_metrics'
end